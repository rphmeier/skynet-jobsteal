An implementation of https://github.com/atemerev/skynet in Rust, using the jobsteal crate for parallelism.

To try it out yourself:
```sh
git clone https://github.com/rphmeier/skynet-jobsteal && cd skynet-jobsteal
cargo run --release
```